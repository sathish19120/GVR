@"
apiVersion: v1
kind: Service
metadata:
  name: gvr-app-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: gvr-app
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
"@ | Out-File -Encoding utf8 -NoNewline k8s/service.yaml
